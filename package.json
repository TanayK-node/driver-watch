{
  "name": "driver-watch-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix driver-watch run dev",
    "build": "npm --prefix driver-watch run build",
    "postinstall": "cd driver-watch && npm install"
  }
}
